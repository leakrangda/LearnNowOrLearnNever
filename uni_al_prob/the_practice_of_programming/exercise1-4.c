//first question
if(c != 'y' || c != 'Y')
	return;

//second question
if(length > BUFSIZE)
	length = BUFSIZE

//third question	
flag = (flag==1) ? 0 : 1

//fourth question
if (*line == '"')
	quote = 1
else
	quote = 0

if ((val & 1) == 1 )
	bit = 1;
else
	bit = 0;
